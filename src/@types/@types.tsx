export type SidebarLinkVariant = 'dashboard' | 'notifications' | 'settings' | 'profile';


export type BandType = {
  name: string;
  description: string;
  style: string;
  city: string;
  id: string;
}
